function headOf(unaLista)  {  var count = 0;  var res = "";  while (unaLista.charAt(count) != ';')    {	res += unaLista.charAt(count); 	count += 1;	}    return res;  }  function tailOf(unaLista)  {  var max = unaLista.length;  var res = "";  for (var i=(unaLista.indexOf(';') + 1);i<max;i++)  {  res = res + unaLista.charAt(i);  }  return res;  }function addTo(object, added)  {  object.value = added + ';' + object.value;  }function remove(elem, object)  {  var temp = object.value;  var res = "";  while (temp.length > 0)    {	if(headOf(temp) != elem) 	  {	  res = res + headOf(temp) + ';';	  }	temp = tailOf(temp);		}  object.value = res;  }function gotElem(elem)  {  var cont = document.getElementById('hiddenShared').value;  var res = false;  while (cont.length > 0)    {	if(headOf(cont) == elem) 	  {	  res = true;	  }	cont = tailOf(cont);		}  return res;  }function frstOf(text){  var count = 0;  var res = "";  while (text.charAt(count) != ',')    {	res += text.charAt(count); 	count += 1;	}    return res;}function scndOf(text){  var max = text.length;  var res = "";  for (var i=(text.indexOf(',') + 1);i<max;i++)  {  res = res + text.charAt(i);  }  return res;}