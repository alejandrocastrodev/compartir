function headOf(unaLista)  {  var count = 0;  var res = "";  while (unaLista.charAt(count) != ';')    {	res += unaLista.charAt(count); 	count += 1;	}    return res;  }  function tailOf(unaLista)  {  var max = unaLista.length;  var res = "";  for (var i=(unaLista.indexOf(';') + 1);i<max;i++)  {  res = res + unaLista.charAt(i);  }  return res;  }function add(added)  {  document.getElementById('hiddenShared').value = added + ';' + document.getElementById('hiddenShared').value;  }function remove(elem)  {  var cont = document.getElementById('hiddenShared').value;  var res = "";  while (cont.length > 0)    {	if(headOf(cont) != elem) 	  {	  res = res + headOf(cont) + ';';	  }	cont = tailOf(cont);		}  document.getElementById('hiddenShared').value = res;  }function gotElem(elem)  {  var cont = document.getElementById('hiddenShared').value;  var res = false;  while (cont.length > 0)    {	if(headOf(cont) == elem) 	  {	  res = true;	  }	cont = tailOf(cont);		}  return res;  }